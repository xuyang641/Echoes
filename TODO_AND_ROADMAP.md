# 遗留问题与未来优化路线图 (Roadmap)

## 🌐 国内部署与数据库迁移计划 (待执行)
> **执行时间**: 下午
> **目标**: 将前后端及数据全面迁移至国内环境，扫清网络延迟与合规障碍，准备进入用户测试阶段。

### 阶段一：对接国内数据库（测试环境跑通）
- [ ] **选项 A：MemFire Cloud (推荐，最省心)**
  - 注册 MemFire Cloud 创建项目。
  - 获取 API URL 和 Anon Key。
  - 修改项目 `.env` 或 `.env.local` 文件：
    ```env
    VITE_SUPABASE_URL=你的MemFire_URL
    VITE_SUPABASE_ANON_KEY=你的MemFire_ANON_KEY
    ```
  - 在 MemFire 的 SQL 编辑器中运行现有的 `supabase_schema.sql`、`create_profiles_table.sql` 等脚本初始化表结构。
- [ ] **选项 B：国内轻量云 + Docker 自建 (最省钱、可备案)**
  - 购买轻量应用服务器（2核2G）。
  - 使用 Docker 启动精简版 Supabase。
  - 修改 `.env` 指向你的服务器 IP/域名。

### 阶段二：前端部署与对象存储对接
- [ ] **图片存储迁移**：
  - 若用 MemFire：直接使用其自带的国内 Storage。
  - 若自建：配置阿里云 OSS SDK 或直接使用自建的本地 Storage。
- [ ] **前端静态资源托管**：
  - 方案 1：Vercel 绑定**已备案的国内域名**以获取国内 CDN 加速。
  - 方案 2（推荐）：将 `npm run build` 生成的 `dist` 丢到阿里云 OSS（开通静态网页托管）或轻量云的 Nginx 中。

### 阶段三：面向用户的测试与上线准备
- [ ] **内测闭环验证 (Alpha Test)**：
  - 验证国内网络环境下，地图瓦片（高德）是否秒开。
  - 验证大体积照片上传的流畅度。
  - 确认手机号/邮箱验证码接收无延迟。
- [ ] **合规性准备**：
  - 准备实名认证域名，并在阿里云/腾讯云提交 **ICP 备案**。
  - 完善并检查《用户协议》与《隐私政策》中文内容。

---

## 🐛 遗留 Bug 记录
### 散落的记忆桌面 (Scattered Memory Desk) 拖拽失效
- **位置**: `src/app/components/scattered-memory-desk.tsx`
- **问题描述**: 目前该组件中照片的拖拽功能无法正常工作。
- **技术原因**: `framer-motion` 的物理动画（`x`, `y` spring）与 `@use-gesture/react` 的事件监听在某些 CSS 定位方式或浏览器事件默认行为（如移动端滚动、原生图片拖拽）下产生了冲突，导致无法正确捕捉和应用位移增量。
- **修复建议**: 
  1. 彻底排查 `useDrag` 的 `event.preventDefault()` 是否正确拦截了浏览器的默认行为。
  2. 尝试不使用 `absolute` 定位结合 `margin` 的 Hack，而是将容器设置为绝对中心，利用 `x` 和 `y` 直接控制相对于中心的绝对偏移。
  3. 检查是否有外层的滚动容器拦截了 pointer 事件。

---

## 🚀 未来四大进阶优化方向

### 1. 产品结构重组（工作台模式 / Dashboard）
- **目标**: 提升桌面端/Pad端的专业感与操作连贯性。
- **方案**: 从目前的“顶部/底部导航 + 平铺页面”重构为经典的三栏/两栏式结构。
  - **左侧边栏**: 全局导航枢纽（所有日记、地图、日历、专集）。
  - **中栏**: 沉浸式内容阅读与编辑区。
  - **右侧面板**: 快捷信息（天气、那年今日、快速记录入口）。

### 2. 全局智能搜索 (Command + K)
- **目标**: 解决历史数据增多后的“检索焦虑”。
- **方案**: 引入类似 Spotlight 的全局搜索面板（快捷键唤起）。
  - 支持全文检索、标签搜索（`#tag`）、地点过滤。
  - **高阶能力**: 结合端侧 AI 模型，直接通过自然语言搜索照片内容（如搜索“海滩”、“猫”找出并未在正文中提及的照片）。

### 3. 日记“专集”管理 (Collections / Albums)
- **目标**: 提供结构化的回忆整理方式，告别单一的流水账时间线。
- **方案**: 
  - 允许用户创建如“2023 关西游”、“宝宝成长记”等主题专集。
  - 日记可以归属到多个专集，专集可自定义精美封面。
  - 专集内部支持按照时间正序/倒序排列，更像一本真实的“故事书”。

### 4. 轻社交与情感共鸣（回音谷 / 漂流瓶）
- **目标**: 在极度私密的产品基因中，提供一个克制、安全的情感释放出口。
- **方案**:
  - 创建一个“匿名广场”或“星空”视图。
  - 用户发布日记时，可选择**脱敏匿名**（仅保留图片、一句话和心情）投递到该板块。
  - **互动克制**: 移除评论和私信功能，用户之间只能通过点击发送“拥抱”或“共鸣”的视觉特效，实现轻量级的情感连接。

### 5. 二次元虚拟陪伴精灵 (Anime Mascot Companion)
- **目标**: 为应用增添情感陪伴价值，提升产品的二次元浓度和用户粘性。
- **方案**:
  - 在页面角落引入一个 2D 二次元吉祥物（如日记精灵）。
  - **互动反馈**: 根据用户当前选择的“心情”、正在打字的行为，或长时间未记录的状态，小人会做出对应的动画反馈（如微笑、递纸巾、举牌子提醒）。
  - **技术落地分期**:
    - *一期方案*: 使用 AI 生成的静态二次元素材，结合 CSS/Framer Motion 实现简单的“呼吸”、“跳跃”等位移动画。
    - *二期方案*: 引入 `pixi-live2d-display` 引擎加载开源 Live2D 模型进行技术验证。
    - *三期方案*: 待获取专属美术资产（原画+骨骼绑定）后，替换为定制的 Live2D 形象，实现转头、视线跟随等深度沉浸互动。
